const RepositoryGeneric = require("./repositoryGeneric")
// dto 


class OrderRepository extends RepositoryGeneric {
    constructor(dao){
        super(dao)
    }
    
   // metodos extras o redefinir
}

module.exports = OrderRepository